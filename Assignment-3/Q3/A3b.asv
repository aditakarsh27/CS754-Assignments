classdef A3b
   methods
       function product = mtimes(A, x)
                x = reshape(x, 217, 217);
                beta = dct2(x);
                product = radon(beta, A.projection_angles);
                product = product(:);
       end
   end
end